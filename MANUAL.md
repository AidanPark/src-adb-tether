# 실사용 매뉴얼 — Every Proxy 방식 (현재 권장)

노트북에서 폰의 무제한 셀룰러 데이터를 쓰되, **통신사 테더링 한도에 차감되지 않게** 하는 방법.
2026-07-06 SKT에서 실기기·실과금 검증 완료. 몇 시간 연속 안정 작동 확인.

> 이 방식이 **현재 권장하는 방법**이다. 초기에 만든 자작 앱(USB) 방식은 [ARCHIVE](#기존-diy-방식과의-관계)로 남겨둔 폴백이다.

---

## 1. 원리 (한 줄)

폰의 **Every Proxy** 앱이 목적지로 **새 소켓을 직접 연다(재생성)** → 통신사엔 "폰의 앱 하나가 데이터 쓰는 것"으로 보임 → 테더링으로 안 잡힘. 자작 앱과 원리는 같고, 검증된 상용 앱으로 대체한 것.

⚠️ 통신사 약관 위반 소지 있음. TTL/DPI 기반 탐지는 무력화되나 **대용량 행위 기반 탐지**는 회피 못 함. 개인 책임.

---

## 2. 구성

```
[노트북 모든 앱] ─(Windows 시스템 프록시)─▶ 폰 핫스팟(WiFi) ─▶ Every Proxy(:8080) ─(재생성)─▶ 셀룰러 ─▶ 인터넷
```

- **폰**: Every Proxy(HTTP 프록시) + 모바일 핫스팟
- **PC**: 폰 핫스팟에 WiFi 연결 + 시스템 프록시를 Every Proxy로 지정
- USB·adb·자작앱·mihomo **불필요**

---

## 3. 요구사항

- 안드로이드 폰, **무제한 데이터 요금제**
- **Every Proxy** 앱 (Play스토어, `com.gorillasoftware.everyproxy`)
- Windows 노트북

---

## 4. 최초 1회 세팅

### 폰
1. **Every Proxy 설치** → 실행
2. **HTTP Proxy** 켜기 — Bind Address `0.0.0.0`, Port `8080`
   - ⚠️ 앱에 뜨는 **Host IP(예 `10.17.56.28`)는 통신사 셀룰러 IP**라 SIM/통신사 바꾸면 사라진다. PC 프록시 주소로는 이걸 쓰지 말고, 아래 §4-7의 **핫스팟 게이트웨이 IP**를 쓴다.
3. **모바일 핫스팟 켜기**
4. **배터리 최적화 예외 (★ 안 하면 백그라운드에서 죽어서 끊김):**
   - 설정 → 앱 → Every Proxy → 배터리 → **제한 없음(Unrestricted)**
   - 설정 → 배터리 → 백그라운드 사용 제한 → **"절대 절전 안 함 앱"에 Every Proxy 추가**
   - 폰 **충전 연결** 권장 (배터리 관리 완화 + 소모 큼)

### PC
5. WiFi로 **폰 핫스팟에 연결**
6. **종량제 설정 (누수 차단):** 설정 → 네트워크 및 인터넷 → Wi-Fi → (해당 네트워크) 속성 → **"데이터 통신 연결" 켬**
   - (윈도우 업데이트·원드라이브·스토어 등 백그라운드가 raw로 새는 걸 막음)
7. **시스템 프록시 설정:** 설정 → 네트워크 및 인터넷 → 프록시 → 수동 프록시 설정 →
   - 주소 `<폰 핫스팟 게이트웨이 IP>`, 포트 `8080` → 저장
   - **게이트웨이 IP 찾기:** 핫스팟에 연결된 PC에서 `ipconfig` → **Wi-Fi 어댑터의 "기본 게이트웨이"** (예: `10.96.115.121`). 이 값은 폰이 부여하므로 통신사와 무관(셀룰러 IP와 달리 SIM 바꿔도 방식 동일). 핫스팟 재시작 시 서브넷이 바뀔 수 있으니 안 되면 재확인.

8. **IPv6 회선(알뜰폰/IPv6 APN)일 때 — raw 누수 차단 (★):**
   폰 회선이 IPv6 기반이면 핫스팟이 PC에 글로벌 IPv6를 그대로 넘겨서, 시스템 프록시를 우회하는 트래픽(특히 **QUIC/UDP**)이 IPv6로 직접 새 **테더링에 차감**된다. 둘 다 꺼서 막는다:
   - **Wi-Fi 어댑터 IPv6 끄기:** 관리자 PowerShell → `Disable-NetAdapterBinding -Name "Wi-Fi" -ComponentID ms_tcpip6`
     (또는 `ncpa.cpl` → Wi-Fi 속성 → "인터넷 프로토콜 버전 6(TCP/IPv6)" 체크 해제. 되돌리기: `Enable-NetAdapterBinding …`)
   - **크롬 QUIC 끄기:** `chrome://flags/#enable-quic` → Disabled → Relaunch (크롬 설치 전체 공통 — 한 번이면 모든 프로필/계정 적용)
   - **판별법:** 핫스팟 연결한 PC에서 `ipconfig` → Wi-Fi에 `2001:…` IPv6 줄이 뜨면 IPv6 회선. 폰 Every Proxy IP 목록에 IPv4 셀룰러(10.x)가 없고 `192.0.0.4/5`(464XLAT)만 보이는 것도 같은 신호. IPv4-only 회선(예전 SKT)에선 이 단계 불필요.

---

## 5. 매일 사용

| | 하는 일 |
|---|---|
| **폰** | Every Proxy 실행 중 + 핫스팟 켜짐 유지 (배터리 예외 걸려 있으면 사실상 방치) |
| **PC** | 핫스팟 연결 + 시스템 프록시 켜짐 → 그냥 평소처럼 사용 |

**모든 노트북 앱**이 시스템 프록시를 타서 폰 회선으로 나감(브라우저 넘어 대부분 앱 커버).

**종료**: PC 프록시를 끄면 됨 — 단, 아래 주의 참고.

---

## 6. 미차감 검증법 (통신사 바꿀 때마다 권장)

1. 폰: 설정 → 연결 → 데이터 사용량 → **"모바일 핫스팟 및 테더링" 값 기록**
2. 노트북에서 프록시 경유로 **수백MB~1GB 사용** (영상·다운로드)
3. 테더링 값 다시 확인:
   - ✅ **거의 안 늘면 성공** (모바일 데이터만 증가)
   - ❌ 사용량만큼 늘면 → 그 통신사는 이 방식이 안 통함

---

## 7. 문제 해결

**노트북 인터넷이 갑자기 안 됨 (에러):**
→ 십중팔구 **Every Proxy가 죽은 것**. 폰에서 **Every Proxy 다시 실행**하면 즉시 복구.
→ ⚠️ **프록시 설정을 끄지 말 것!** 끄면 인터넷은 돌아오지만 모든 트래픽이 raw로 나가 **테더링에 차감**됨(조용히 쿼터 소모). 반드시 앱을 다시 켜서 해결.

**자주 죽음:** 배터리 예외(§4-4)를 다시 확인. "절대 절전 안 함 앱" 등록이 핵심.

**느림:** 폰 핫스팟을 **5GHz**로 변경 (2.4GHz는 느림). 폰 핫스팟 설정 → 밴드 → 5GHz.

**특정 앱만 안 됨:** 그 앱이 시스템 프록시를 무시하는 경우(일부 게임 등). 이런 앱은 이 방식으로 커버 안 됨.

---

## 8. 한계 / 주의

- **누수 ~미미하지만 0은 아님**: 시스템 프록시를 무시하는 OS/앱 트래픽은 여전히 raw로 새서 테더링에 소량 잡힘(검증 시 1GB당 ~20MB). 종량제 설정으로 최소화됨.
- **TCP 위주**: HTTP 프록시라 TCP 기반(브라우징·API·다운로드)은 문제없음. 순수 UDP 앱은 제한될 수 있음.
- **DNS 누수 없음**: HTTP 프록시가 CONNECT로 호스트명을 받아 폰이 셀룰러에서 해석.
- **통신사 의존**: 미차감 여부는 통신사 탐지 방식에 달림 → 통신사 바꾸면 §6 재검증.
- **약관 위반 소지 / 행위 기반 탐지**는 회피 못 함.

---

## 9. 통신사 변경 시

- 프록시 주소는 **핫스팟 게이트웨이 IP**(§4-7)를 쓰면 통신사 무관. ⚠️ 앱의 셀룰러 Host IP(`10.x` 등)를 적어놨다면 SIM 바꿀 때 사라지니 반드시 게이트웨이 IP로 교체(2026-07-09 알뜰폰 전환 때 이걸로 접속 깨졌음).
- 새 통신사가 **IPv6 회선이면 §4-8**(Wi-Fi IPv6·크롬 QUIC 끄기) 반드시 적용 — 안 하면 QUIC/IPv6로 raw 누수.
- 새 통신사 APN은 자동 잡힘
- **§6 미차감 검증**을 다시 돌려 확인
- 새 요금제가 테더링도 넉넉하면 이 방식 자체가 불필요할 수도 있음

---

## 검증 이력

- **2026-07-06 / SKT / SM-S947N (OneUI 8.5)**: 프록시 경유 1GB → 테더링 10.01→10.03GB(**+0.02GB, 사실상 flat**), 모바일 데이터만 +1.16GB. 누수 ~20MB. 몇 시간 연속 안정 작동.
- **2026-07-09 / 알뜰폰(IPv6 회선, prefix `2001:2d8` = KT계열 추정) / SM-S947N**: SIM 교체 후 폰에 IPv4 셀룰러 주소가 안 잡히고 `192.0.0.x`(464XLAT)만 → 기존 프록시 주소(`10.17.56.28`)가 죽어 raw 누수 발생. **프록시 주소를 핫스팟 게이트웨이 IP(`10.96.115.121`)로 교체 + Wi-Fi IPv6·크롬 QUIC OFF**로 정상화. 프록시 경유 **0.94GB가 Every Proxy 앱 데이터로 집계(미차감 재확인)**, 테더링 누적 93.35MB. ※ 이 테더링 값은 교체 전 raw 누수 구간이 포함된 누적치라 순수 누수율 아님 — 정밀 측정은 정상화 후 before→after로 재측정 권장.

---

## 기존 DIY 방식 (git 히스토리에 보존)

초기엔 자작 안드로이드 SOCKS 서버 앱 + USB(`adb forward`) + mihomo로 구현했다. USB 전송이라 softap 누수가 원천적으로 없는 **더 청정한 방식**이지만, 자작앱 유지보수·크롬 동시연결 느림·세팅 복잡이 단점이라, 더 간단한 Every Proxy 방식으로 주력을 전환하고 저장소를 이 매뉴얼 하나로 정리했다.

DIY 방식 전체(android 앱 소스, windows/client mihomo, 설계·검증 문서)는 **git 히스토리 커밋 `44d9f1a`에 보존**돼 있다. 새 통신사에서 이 방식이 안 통해 폴백이 필요하면 아래로 복원:

```bash
git checkout 44d9f1a -- android windows README.md HANDOFF.md ROADMAP.md
```

배경·검증 결과는 프로젝트 메모리 `every-proxy-pivot` 참조.

# 실사용 매뉴얼 — USB + TUN 방식 (현재 권장)

노트북에서 폰의 무제한 셀룰러 데이터를 쓰되, **통신사 테더링 한도에 차감되지 않게** 하는 방법.
**2026-07-10** 알뜰폰(KT계열)에서 **Windows·WSL 전 트래픽 캡처 실측 검증 완료.**

> 구방식(폰 핫스팟 + Windows 시스템 프록시)은 **WSL·CLI·QUIC 누수**와 **핫스팟 IP 변덕** 때문에 폐기했다. → [§부록](#부록-구방식-핫스팟--시스템-프록시-폐기)

⚠️ 통신사 약관 위반 소지 있음. TTL/DPI 기반 탐지는 무력화되나 **대용량 행위 기반 탐지**는 회피 못 함. 개인 책임.

---

## 1. 원리

두 층으로 나뉜다.

| 층 | 역할 |
|---|---|
| **폰쪽 (재생성)** | 폰의 프록시 앱이 목적지로 **새 소켓을 직접 연다** → 통신사엔 "폰의 앱 하나가 데이터 쓰는 것"으로 보임 → 테더링 미집계 |
| **PC쪽 (전체 캡처)** | mihomo **TUN**이 PC의 **모든** 트래픽을 호스트 레벨에서 잡아 **USB(`adb forward`)** 로 폰 프록시에 넘김 |

**왜 TUN인가** — Windows 시스템 프록시는 WinINET 앱(브라우저 등)만 태운다. **WSL2 리눅스·CLI 도구(git·npm·pip·Claude Code)·QUIC/UDP는 전부 우회**해서 raw로 나가 테더링에 차감됐다. TUN은 호스트 레벨 캡처라 **WSL 포함 전부** 잡는다.

**왜 USB인가** — 핫스팟 IP는 재시작마다 랜덤화된다(실제로 하루에 `10.96.115.121` → `10.231.204.173` → `10.140.82.155` 3번 바뀜). USB는 `127.0.0.1` 고정이라 **영구히 안 깨진다.** 테더링 인터페이스를 안 쓰니 softap 누수도 원천 차단.

---

## 2. 구성

```
[PC 모든 앱 + WSL] ─▶ mihomo TUN ─▶ 127.0.0.1:8080 ─(USB/adb forward)─▶ 폰 프록시 ─(재생성)─▶ 셀룰러
                          │
                          └─ exceptions.yaml 등록 프로세스만 ─▶ PC 기존 회선(Direct)
```

- **폰**: 프록시 앱(현재 **Every Proxy** HTTP `:8080`) + USB 디버깅
- **PC**: `windows/client/run.bat` (관리자) → `adb forward` + mihomo TUN
- **핫스팟 불필요** — 데이터는 USB로 흐른다. 오히려 **꺼야** 누수 경로가 사라진다 → [§4-6](#pc)

---

## 3. 요구사항

- 안드로이드 폰, **무제한 데이터 요금제**, USB 디버깅 가능
- 폰 프록시 앱 — **Every Proxy** (`com.gorillasoftware.everyproxy`)
  - 또는 자작 SOCKS5 앱(`android/`) — [§11](#11-every-proxy-의존-제거-자작앱-전환)
- Windows 노트북 + **관리자 권한**(TUN 드라이버) + **adb**(Android SDK platform-tools)
- 데이터 전송용 USB 케이블

---

## 4. 최초 1회 세팅

### 폰
1. **Every Proxy 설치 → 실행 → HTTP Proxy 켜기**
   - Bind Address `0.0.0.0`, Port `8080`
   - ⚠️ 앱에 뜨는 Host IP는 **볼 필요 없다** (USB 방식은 IP를 안 씀)
2. **USB 디버깅 ON** — 설정 → 개발자 옵션 → USB 디버깅
3. **배터리 최적화 예외 (★ 안 하면 백그라운드에서 죽어 끊김):**
   - 설정 → 앱 → Every Proxy → 배터리 → **제한 없음(Unrestricted)**
   - 설정 → 배터리 → **"절대 절전 안 함 앱"에 Every Proxy 추가**
   - 앱 상단에 **"Application Settings — Action required"** 배너가 뜨면 = 이 예외가 안 걸린 것. **Fix** 눌러 해결.

### PC
4. **adb 확인** — `adb devices` 에 기기가 `device` 로 뜨면 OK
   (폰에 "USB 디버깅 허용?" 뜨면 승인)
5. **Windows 수동 프록시 OFF** — 설정 → 네트워크 및 인터넷 → 프록시
   → **TUN이 대체하므로 반드시 꺼야 한다** (켜두면 경로가 이중이 되어 혼란)
6. **폰 핫스팟은 끄는 것을 권장 (★ 최적 구성)** — 데이터는 USB로 흐르므로 핫스팟이 필요 없다.
   - **2026-07-10 검증**: PC에 **물리 NIC이 하나도 없는 상태**(Wi-Fi 끊김, mihomo TUN `Meta`만 존재)에서 Windows·WSL 모두 정상 egress.
   - **장점**: 테더링 인터페이스 자체가 없으니 **누수가 구조적으로 불가능**하다(fail-closed — 새는 대신 실패). 핫스팟 IP 변덕·IPv6 누수·QUIC raw 유출이 전부 무의미해진다. 폰 배터리도 절약.
   - ⚠️ **예외 기능(`exceptions.yaml`)을 쓸 때만** 물리 회선이 필요하다 — DIRECT 트래픽이 나갈 곳이 있어야 하므로 PC를 **독립 회선(집 WiFi)** 에 연결할 것. (폰 핫스팟에 붙이면 예외 트래픽이 테더링에 잡혀 의미가 없다)

---

## 5. 매일 사용

| | 하는 일 |
|---|---|
| **폰** | USB 연결 + Every Proxy 실행 유지 (배터리 예외 걸려 있으면 사실상 방치) |
| **PC** | `windows\client\run.bat` **더블클릭 → UAC 허용** → 검은 창에 로그 흐르면 가동 |

`run.bat` 이 자동으로: 관리자 승격 → `adb forward tcp:8080 tcp:8080` → `mihomo.exe -d .` 실행.

**종료**: 그 **검은 창을 닫으면** TUN 경로가 해제되고 즉시 원복된다.

---

## 6. 검증법

### 6-1. IP 지표 (즉시 판정 — 가장 유용)

폰이 **APN을 나눠 쓰기 때문에**, 재생성 경로와 테더 경로의 **공인 IP가 다르다.** 이걸로 한 방에 판정한다.

**기준값 얻기** (TUN 끈 상태에서, `adb forward tcp:8080 tcp:8080` 만 걸고):
```cmd
curl.exe -s -x http://127.0.0.1:8080 https://api.ipify.org   REM 프록시 경유 = 미차감 IP
curl.exe -s https://api.ipify.org                            REM raw = 테더 IP
```

**판정** (TUN 켠 상태에서):
```cmd
curl.exe -s https://api.ipify.org
```
```bash
# WSL
curl -s https://api.ipify.org
```
- **둘 다 "프록시 경유 IP"와 같으면 → 성공** (전 트래픽이 재생성 경로)
- **"raw IP"가 나오면 → 그 경로는 새는 중**

> 이 기기 실측 예: 프록시 `223.38.72.253` / raw `223.38.73.210` — 앞 3옥텟이 갈린다. IP는 회선 재접속 시 바뀌므로 **매번 기준값을 다시 뽑아 쓸 것.**

### 6-2. 실과금 검증 (통신사 바꿀 때마다 권장)

1. 폰 → 설정 → 데이터 사용량 → **"모바일 핫스팟 및 테더링" 값 기록**
2. TUN 켠 채로 수백MB~1GB 사용 (WSL 작업 포함하면 더 좋음)
3. 다시 확인:
   - ✅ **테더링 flat + 프록시 앱 데이터만 증가** → 성공
   - ❌ 사용량만큼 늘면 → 그 통신사는 이 방식이 안 통함

---

## 7. 예외 관리 (PC 기존 회선으로 뺄 프로그램)

`windows/client/exceptions.yaml` 의 `payload:` 아래에 프로세스명으로 추가:

```yaml
payload:
  - PROCESS-NAME,OneDrive.exe
  - PROCESS-NAME,steam.exe
  - PROCESS-NAME,tailscaled.exe
```

편집 후 **mihomo 재시작**(창 닫고 `run.bat` 다시)하면 적용.

기본이 "전부 폰"이라 **볼륨 큰 배경 프로그램은 예외로 빼는 걸 권장**한다.

---

## 8. 문제 해결

**인터넷이 갑자기 안 됨**
→ 십중팔구 **폰 프록시 앱이 죽은 것**. 폰에서 Every Proxy 다시 실행. 재발하면 §4-3 배터리 예외 재확인.
→ 급하면 mihomo 창을 닫으면 즉시 원복(단, 그 상태는 전부 raw = 차감).

**`adb devices` 가 비어 있음**
→ USB 케이블(데이터용인지) · USB 디버깅 ON · 폰의 "USB 디버깅 허용?" 승인 확인.
→ `adb kill-server && adb devices` 로 재시도.

**TUN이 안 잡는 것 같음 (IP가 raw로 나옴)**
→ Windows 수동 프록시가 아직 켜져 있는지 확인(§4-5). mihomo 창에 에러 로그 없는지 확인.

**특정 앱만 느리거나 안 됨**
→ UDP 기반일 가능성. `exceptions.yaml` 로 빼거나 §9 참조.

---

## 9. 한계 / 주의

- **TCP 위주**: HTTP 프록시라 TCP만 중계. **QUIC/UDP는 폴백 또는 차단**된다.
  - **Tailscale**(WireGuard UDP)은 직결이 막혀 DERP(TCP) 폴백으로 느려질 수 있음 → 필요하면 예외 등록.
  - 브라우저는 QUIC을 꺼두면 안정적: `chrome://flags/#enable-quic` → Disabled (크롬 설치 전체 공통, 한 번이면 모든 프로필 적용)
- **배경 트래픽도 전부 폰 데이터**를 쓴다(기본이 폰). 무제한 전제.
- **관리자 권한 + WinTun 드라이버** 필수.
- **DNS 누수 없음**: fake-ip + 폰이 CONNECT 호스트명을 셀룰러에서 해석.
- **통신사 의존**: 미차감 여부는 통신사 탐지 방식에 달림 → 통신사 바꾸면 §6-2 재검증.
- **약관 위반 소지 / 행위 기반 탐지**는 회피 못 함.

---

## 10. 통신사 변경 시

- **세팅 그대로 재사용** — USB 방식은 `127.0.0.1` 고정이라 통신사·IP와 무관하다. (구방식과 달리 고칠 게 없음)
- 새 통신사 APN은 자동 잡힘
- **§6 검증만 다시** 한 번 (기준 IP 재취득 + 실과금 확인)
- 새 요금제가 테더링도 넉넉하면 이 방식 자체가 불필요할 수도 있음

---

## 11. Every Proxy 의존 제거 (자작앱 전환)

현재 폰쪽 서버로 상용 앱(Every Proxy)을 쓰지만, **필수가 아니다.** TUN·USB 전송은 동일하고 폰쪽 리스너만 바뀐다.

자작 SOCKS5 앱(`android/`)으로 전환하려면:
1. `android/` 빌드 → APK 설치 → 앱에서 "시작"
2. `windows/client/config.yaml` 의 `proxies:` 를 주석 처리된 **socks5 블록**으로 교체
3. `windows/client/run.bat` 의 `adb forward` 포트를 **1080**으로

**자작앱 장점**: third-party·광고 없음, 부팅 자동시작(BootReceiver) 구현돼 있어 무인 상시구동에 오히려 견고.
**단점**: 직접 유지보수.

---

## 검증 이력

- **2026-07-03 / KT계열 / R3KL20CDTBD**: 자작 SOCKS5 앱 + adb forward + mihomo TUN. 블랙리스트 라우팅·예외파일 동작 확인(기본→폰, 예외 `curl.exe`→기존회선).
- **2026-07-06 / SKT / SM-S947N (OneUI 8.5)**: **구방식**(핫스팟+시스템프록시) 프록시 경유 1GB → 테더링 10.01→10.03GB(사실상 flat). 누수 ~20MB.
- **2026-07-09 / 알뜰폰(IPv6 회선) / SM-S947N**: **구방식**의 한계 노출. SIM 교체로 셀룰러 IP가 사라져 접속 붕괴, 핫스팟 IP가 하루 3번 변동, **WSL·CLI 트래픽이 시스템 프록시를 우회해 지속 누수**. → USB+TUN 방식으로 전환 결정.
- **2026-07-10 / 알뜰폰(KT계열) / R3KL20CDTBD**: **USB+TUN 검증 완료.** `adb forward tcp:8080` 경유 HTTP 200. TUN 가동 후 **Windows `223.38.72.253` / WSL `223.38.72.253`** 로 **양쪽 모두 재생성 경로 확인**(raw였다면 `223.38.73.x`). **그동안 새던 WSL 누수 해소.**
- **2026-07-10 (추가) / 핫스팟 OFF 구성 검증**: 폰 핫스팟을 끄고 **PC에 물리 NIC이 하나도 없는 상태**(활성 인터페이스가 mihomo TUN `Meta` + 가상 어댑터뿐)에서도 **Windows·WSL·프록시 직접경유 세 경로 모두 `223.38.72.253`** 로 일치. → **핫스팟 OFF가 최적 구성**(테더링 인터페이스 부재 = 누수 구조적 불가). 사전 우려였던 "underlying 인터페이스 필요"는 실사용상 무영향(예외 목록이 비어 있고 프록시는 loopback 경유).

---

## 부록: 구방식 (핫스팟 + 시스템 프록시, 폐기)

```
[PC WinINET 앱] ─(Windows 시스템 프록시)─▶ 폰 핫스팟(WiFi) ─▶ Every Proxy(:8080) ─▶ 셀룰러
```

PC를 폰 핫스팟에 WiFi로 연결하고 Windows 수동 프록시를 `<핫스팟 게이트웨이 IP>:8080` 으로 지정하는 방식. 관리자 권한·USB가 필요 없어 간편했다.

**폐기 이유:**
1. **커버리지 공백** — WinINET 앱만 커버. **WSL2·CLI 도구(git/npm/Claude Code)·QUIC/UDP는 전부 raw로 누수** → 테더링 차감. 이게 "프록시 쓰는데 왜 테더링이 계속 늘지?"의 정체였다.
2. **IP 파편성** — 핫스팟 게이트웨이 IP가 재시작마다 랜덤화(하루 3번 변동 관측). 갱신을 놓치면 **조용히 전부 raw로** 새며 차감.
3. **IPv6 회선 누수** — 알뜰폰 등 IPv6 회선은 핫스팟이 PC에 글로벌 IPv6를 넘겨 QUIC/UDP가 직접 새나감. Wi-Fi IPv6·QUIC을 따로 꺼야 했다.

간편함이 필요해 이 방식을 쓸 경우의 완화책: Wi-Fi를 **종량제**로 설정, Wi-Fi 어댑터 **IPv6 끄기**(`Disable-NetAdapterBinding -Name "Wi-Fi" -ComponentID ms_tcpip6`), 크롬 **QUIC 끄기**. 그래도 WSL·CLI 누수는 못 막는다.

상세 절차는 git 히스토리 `9dbc9df` 의 MANUAL.md 참조.
